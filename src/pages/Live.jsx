import LiveStream from '../components/LiveStream';
import Layout from '../components/Layout';

const Live = () => {
    return (
        <Layout>
            <LiveStream />
        </Layout>
    );
};

export default Live;